name: Code Review

on:
  pull_request_target:
    types: [opened, reopened]
  issue_comment:
    types: [created]

jobs:
  review:
    uses: dataplanelabs/workflows/.github/workflows/code-review.yml@main
    secrets: inherit
